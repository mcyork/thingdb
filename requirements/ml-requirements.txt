# OPTIMIZED ML Requirements - Saves ~5GB+ by using CPU-only versions
# Total size reduction: ~80% smaller than GPU versions

# System monitoring (lightweight)
psutil

# Math libraries (pinned to reasonable versions)
numpy

# CPU-ONLY PyTorch (MASSIVE size savings - no CUDA libraries)
# This single change saves ~3-4GB!
# Updated to 2.1.1+ to support sentence-transformers requirements
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.1+cpu
torchvision==0.16.1+cpu
torchaudio==2.1.1+cpu

# Lightweight sentence transformers (requires PyTorch >= 2.1)
# Updated to newer version for huggingface_hub compatibility
sentence-transformers==2.7.0

# Skip scikit-learn entirely - not needed for sentence transformers
# scikit-learn==1.3.0  # Only add if you actually use it

# Alternative: Use sentence-transformers-lite if available
# sentence-transformers==2.2.2 --no-deps
# transformers==4.30.2
# huggingface-hub==0.15.1